# Superseded

`BUILD_SPEC.md` (draft) and `SPEC_REVIEW.md` (its review) were merged into `../SPEC.md`
on 2026-08-06. The review's D1–D6 are resolved in `SPEC.md` §11.

Kept only until the merge is committed — neither file was ever in git, so deleting them
before the commit would be unrecoverable. Delete this folder after the first commit that
contains `SPEC.md`.
